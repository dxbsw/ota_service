# OTA Service 使用说明

本文档面向“把 `ota_service` 作为独立组件接入到其他 ESP-IDF 工程”的场景。

如果你只想快速接入，按下面 6 步即可。

## 1. 添加组件依赖

如果通过组件中心依赖管理，工程侧可在 `idf_component.yml` 中添加：

```yaml
dependencies:
  dxbsw/ota-service:
    version: "^1.0.2"
```

如果组件已作为本地目录放在 `components/ota_service`，则只需保证其依赖可解析。

`ota_service` 当前依赖：

- `joltwallet/littlefs:^1.21.1`
- `dxbsw/version_checker:^1.0.0`
- `json`
- `app_update`
- `esp_http_client`
- `esp_timer`
- `mbedtls`

## 2. 准备分区表

建议至少包含以下分区：

```csv
ota_0,       app,  ota_0,      0x10000,  0x400000,
ota_1,       app,  ota_1,      0x410000, 0x400000,
models,      data, ,           0x810000, 0x200000,
assets,      data, littlefs,   0xA10000, 0x5A3000,
assets_meta, data, littlefs,   0xFB3000, 0xA000,
```

说明：

- `ota_0` / `ota_1`：固件双分区
- `assets`：LittleFS 资源分区
- `assets_meta`：本地 `manifest.json` / `state.json` / `integrity.json`
- `models`：可选，用于 `mmap` 原始分区资源

如果你的项目没有模型资源，`models` 可以先保留空分区，也可以按项目实际调整。

## 3. 准备服务端

设备只访问一个统一地址：

```text
http://<server-ip>:8000/ota/metadata
```

该地址返回一个总 JSON，内部包含：

- `firmware`
- `manifest`

资源文件下载地址由组件内部自动推导：

- `assets` 资源：`/assets{path}`
- `models` 资源：`/models{path}`

如果直接使用本组件自带测试服务端，可参考：

- `python_tool/ota_server.py`
- `python_tool/ota_server_config.json`

## 4. 主程序最小接入

最小示例：

```c
#include "freertos/FreeRTOS.h"
#include "freertos/task.h"
#include "esp_err.h"
#include "ota_service.h"

void app_main(void)
{
    ota_service_config_t config = ota_service_get_default_config();
    ota_service_status_t status = {0};

    config.metadata_url = "http://192.168.3.198:8000/ota/metadata";
    config.auto_reboot = true;

    ESP_ERROR_CHECK(ota_service_start(&config));

    while (!ota_service_is_finished()) {
        if (ota_service_get_status(&status) == ESP_OK) {
            printf("busy=%d need_ota=%d target=%s step=%u/%u speed=%u B/s\n",
                   status.busy,
                   status.need_ota,
                   status.current_target,
                   (unsigned int)status.completed_steps,
                   (unsigned int)status.total_steps,
                   (unsigned int)status.current_speed_bytes_per_sec);
        }
        vTaskDelay(pdMS_TO_TICKS(1000));
    }
}
```

接入要求：

- 在调用 `ota_service_start()` 前，设备必须已经联网
- `metadata_url` 必须可访问
- 若使用 HTTPS，再按需配置 `server_cert_pem`

## 5. 常用配置项

`ota_service_get_default_config()` 返回默认配置，通常只需要改这几个：

- `metadata_url`：统一 OTA 地址
- `auto_reboot`：是否自动重启
- `http_timeout_ms`：HTTP 超时
- `download_buffer_size`：下载缓冲大小
- `worker_stack_size`：后台 OTA task 栈大小

典型写法：

```c
ota_service_config_t config = ota_service_get_default_config();

config.metadata_url = "http://192.168.3.198:8000/ota/metadata";
config.auto_reboot = true;
config.http_timeout_ms = 5000;
config.download_buffer_size = 8192;
```

补充说明：

- 第一次调用 `ota_service_start()` 会完成初始化并保存当前配置
- 如果组件已初始化且后台 OTA task 未运行，再次调用 `ota_service_start()` 可以更新运行配置并启动新一轮检查
- 重新启动前允许更新的字段主要是：`metadata_url`、`server_cert_pem`、`http_timeout_ms`、`worker_stack_size`、`worker_priority`、`download_buffer_size`、`skip_cert_common_name_check`、`auto_reboot`
- 如果要修改 `assets_partition_label`、`assets_meta_partition_label`、`assets_base_path`、`assets_meta_base_path` 这类挂载参数，需要先调用 `ota_service_deinit()`

## 6. 查询运行状态

主程序推荐周期性轮询 `ota_service_get_status()`。

常用字段：

- `runtime_state`：当前阶段
- `transaction_state`：当前事务状态
- `busy`：是否仍在执行
- `need_ota`：是否需要 OTA
- `success`：本轮是否成功
- `reboot_pending`：是否等待重启
- `current_target`：当前目标
- `current_file_downloaded` / `current_file_size`：当前文件进度
- `current_speed_bytes_per_sec`：当前速度
- `last_error`：最近错误

## 7. 组件内部做了什么

调用 `ota_service_start()` 后，组件内部会自动：

1. 初始化并挂载 `assets` / `assets_meta`
2. 准备 `manifest.json` / `state.json` / `integrity.json`
3. 创建后台 OTA worker task
4. 访问 `metadata_url`
5. 比较固件版本和资源版本
6. 启动时校验本地 `models` 完整性，异常时强制对应资源重新 OTA
7. 按需执行固件 OTA，并在写入完成后再次校验目标 OTA 分区
8. 按需执行资源逐文件 OTA
9. 更新本地 `manifest.json` 与 `integrity.json`
10. 整轮 OTA 成功后才切换启动分区，并根据配置决定是否自动重启

你不需要自己处理下载、写入、哈希校验和状态维护。

## 8. 可选功能：本地初始资源基线

如果你的设备在首次启动时就希望拥有一份“本地默认资源版本”，可以在调用 `ota_service_start()` 之前，先自行准备一个本地资源包并调用：

```c
esp_err_t ota_service_apply_local_package(const ota_service_local_package_t *package,
                                          ota_service_update_plan_t *plan_out);
```

适用场景：

- 固件内嵌了初始资源
- 首次启动时先把初始资源写入 `assets`
- 后续 OTA 再基于这份本地基线和服务端比较

这个模式在当前工程示例中已经验证通过。

## 9. 版本号要求

版本比较使用 `dxbsw/version_checker`，因此版本号建议统一使用：

- `1.0.0`
- `1.0.0.1`

不建议使用：

- `v1.0.0`
- `1.0.0-beta`
- `release-1`

## 10. 常见问题

### 10.1 服务端没启动

现象：

- `ota_service_get_status()` 中 `last_error` 为 `ESP_ERR_HTTP_CONNECT`

排查：

- 检查设备网络是否已连通
- 检查 `metadata_url` 是否正确
- 检查服务端是否已启动

### 10.2 OTA 完成后没有自动重启

排查：

- 检查 `config.auto_reboot` 是否为 `true`
- 检查本轮是否真的发生了固件或资源更新

### 10.3 第一次启动没有资源基线

说明：

- 组件默认会准备 `manifest.json`、`state.json`、`integrity.json`
- 这三个文件缺失或损坏时，会自动按默认值重建，其中默认版本为 `0.0.0`
- 如果项目需要初始资源基线，需要上层主动调用 `ota_service_apply_local_package()`

### 10.4 为什么固件下载成功后没有立刻切换分区

说明：

- 当前组件会先完成整轮 OTA，包括资源更新和完整性校验
- 只有当固件、资源、`models` 校验都成功后，才会切换启动分区
- 这样可以避免出现“固件成功、资源失败，但下次重启静默进入新固件”的不一致状态

## 11. 相关文档

- 总览说明见 `README.md`
- 设计细节见 `OTA开发文档.md`
- 对外 API 见 `include/ota_service.h`
